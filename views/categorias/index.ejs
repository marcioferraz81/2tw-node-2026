<!DOCTYPE html>
<html>
<head>
    <title>Categorias</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/nav') %>
    <div class="container mt-4">
        <!-- FORMULÁRIO -->
        <div class="card shadow-sm mb-4">
            <div class="card-body">

                <h5 class="mb-3"><%= categoriaEditar ? '✏️ Editar' : '➕ Nova' %> Categoria</h5>

                <form action="<%= categoriaEditar ? '/categorias/editar/' + categoriaEditar.id : '/categorias/salvar' %>" method="POST">

                    <div class="mb-3">
                        <label class="form-label">Nome</label>
                        <input type="text" name="nome" class="form-control form-control-lg" required value="<%= categoriaEditar ? categoriaEditar.nome : '' %>">
                    </div>

                    <div class="d-grid">
                        <button class="btn btn-<%= categoriaEditar ? 'success' : 'primary' %>"><%= categoriaEditar ? 'Atualizar' : 'Salvar' %></button>

                        <% if(categoriaEditar){ %>

                            <a href="/categorias" class="btn btn-secondary mt-2">
                                Cancelar
                            </a>

                        <% } %>
                    </div>

                </form>

            </div>
        </div>

        <!-- LISTAGEM -->
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h4 class="mb-0">📂 Categorias</h4>
        </div>

        <!-- 🖥️ DESKTOP -->
        <div class="d-none d-md-block">
            <table class="table table-bordered table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Nome</th>
                        <th width="120">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <% categorias.forEach(cat => { %>
                        <tr>
                            <td><%= cat.id %></td>
                            <td><%= cat.nome %></td>
                            <td>
                                <a 
                                    href="/categorias/editar/<%= cat.id %>" 
                                    class="btn btn-warning btn-sm"
                                >
                                    ✏️
                                </a>
                                <a href="/categorias/excluir/<%= cat.id %>" 
                                   class="btn btn-danger btn-sm"
                                   onclick="return confirm('Deseja excluir esta categoria?')">
                                    🗑️
                                </a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

        <!-- 📱 MOBILE -->
        <div class="d-md-none">
            <% categorias.forEach(cat => { %>
                <div class="card mb-3 shadow-sm">
                    <div class="card-body">

                        <h5 class="card-title"><%= cat.nome %></h5>

                        <p class="card-text text-muted">
                            ID: <%= cat.id %>
                        </p>

                        <div class="d-grid">
                            <a 
                                    href="/categorias/editar/<%= cat.id %>" 
                                    class="btn btn-warning btn-sm"
                                >
                                    ✏️
                                </a>
                            <a href="/categorias/excluir/<%= cat.id %>" 
                               class="btn btn-danger btn-sm"
                               onclick="return confirm('Deseja excluir esta categoria?')">
                                ❌ Excluir
                            </a>
                        </div>

                    </div>
                </div>
            <% }) %>
        </div>

        <a href="/" class="btn btn-secondary w-100 mt-3">Voltar</a>

    </div>

    <%- include('../partials/footer') %>

</body>

</html>